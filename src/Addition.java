
public class Addition {

	//Takes in first number and second number, then adds them together and return the summation
	public int addition(int firstNumber, int secondNumber) {
		return (firstNumber + secondNumber);
	}
}
